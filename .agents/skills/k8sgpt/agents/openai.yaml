interface:
  display_name: "K8sgpt"
  short_description: "Repo-specific patterns and workflows for k8sgpt"
  default_prompt: "Use the k8sgpt repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true